"""
make_time/prompts.py — 所有 AI 提示词构建函数
每个函数接收变量，返回完整 prompt 字符串。
"""


def build_match_subtitle_prompt(yuan_text, zimu_union_text):
    """
    阶段1：给定原文句子（含参考时间点），从候选字幕中找出匹配的 id_list。
    适用于文案有时间轴辅助定位的场景。
    """
    example = '''
[原文句子]
西方用SWOT分析 (00:03:40,100)

[时间序列]
104
00:03:25,100 --> 00:03:27,833
真正的你要知道敌人是什么


105
00:03:28,133 --> 00:03:29,866
你才有你的SWOT


108
00:03:32,433 --> 00:03:34,066
为什么西方用这个SWOT


[正确输出]
{
    "id_list": [108],
    "text": "为什么西方用这个SWOT"
}
'''
    return f'''请根据以下规则整理出与原文句子相关的时间序列并生成结构化JSON数据：
# 处理规则
1. 输入数据：
   - [时间序列]：字幕片段（含字幕id,时间戳和文本）
   - [原文句子]：需要匹配的目标句子（含参考时间点）

2. 匹配要求：
   - 将[时间序列]中与[原文句子]语义匹配的连续片段合并为一个条目
   - 合并的这个条目和原文句子精确匹配

3. JSON格式规范：
   - id_list: 匹配字幕的id列表
   - text: 合并后的完整文本（空格连接）
{example}
# 待处理数据
[原文句子]
{yuan_text}

[时间序列]
{zimu_union_text}

请严格按照示例格式输出JSON，确保：
1. id_list, 合并的text 和 原文句子是匹配的
2. 文本内容用单个空格连接片段
3. 不使用换行符或特殊分隔符'''


def build_find_subtitle_prompt(yuan_text, zimu_union_text):
    """
    阶段2（降级）：无时间轴辅助时，从全文字幕中语义搜索匹配片段。
    允许跳跃式拼接（非连续 id）。
    """
    example = '''
[原文句子]
企业扩张时要学会就地取材解决补给问题

[时间序列]
403
00:11:30,933 --> 00:11:31,666
所以一个企业

406
00:11:35,200 --> 00:11:36,300
尤其要扩张时候

410
00:11:39,633 --> 00:11:40,266
一定要学会

433
00:12:15,700 --> 00:12:16,633
就地取材

379
00:10:45,433 --> 00:10:47,000
他是彻底的解决补给问题

[正确输出]
{
    "id_list": [403, 406, 410, 433, 379],
    "text": "所以一个企业 尤其要扩张时候 一定要学会 就地取材 他是彻底的解决补给问题"
}
'''
    return f'''请根据以下规则整理出与原文句子相关的时间序列并生成结构化JSON数据：
# 处理规则
1. 输入数据：
   - [时间序列]：字幕片段（含字幕id,时间戳和文本）
   - [原文句子]：需要匹配的目标句子

2. 匹配要求：
   - 在时间序列里找若干子序列拼接后语义匹配原文句子
   - 允许跳跃选取（id 不必连续）

3. JSON格式规范：
   - id_list: 匹配字幕的id列表（顺序与text对应）
   - text: 拼接后的完整文本（空格连接）
{example}
# 待处理数据
[原文句子]
{yuan_text}

[时间序列]
{zimu_union_text}

请严格按照示例格式输出JSON，确保：
1. id_list里面的id要仔细核对和时间序列里面的id一致
2. 拼接后的text 和 原文句子是匹配的
3. 文本内容用单个空格连接片段
4. 不使用换行符或特殊分隔符'''


def build_check_similarity_prompt(result_text, yuan_text):
    """
    相似度校验：比较 AI 返回的字幕文本与原文句子的相似程度。
    返回 probability（0.0-1.0）。
    """
    return f'''请比较2个句子的相似性，输出结构化JSON数据：
# 处理规则
1. 比较[句子1]和[句子2]的相似程度
2. JSON格式：probability（范围0.0-1.0，保留2位小数）

# 示例输入：
[句子1]：因为在上市公司我 我做事做得很好 我在那都是做的最好的 但是那个时候会觉得 这好像都是自己的能力 其实有很多是平台的能力
[句子2]：因为在上市公司我做事做得很好，我在那都是做的最好的，但是那个时候会觉得这好像都是自己的能力，其实有很多是平台的能力
[正确输出]
{{"probability": 0.95}}

# 示例输入2：
[句子1]：SWOT是极小极小的工具
[句子2]：sort是一个极小极小 极小的工具
[正确输出]
{{"probability": 0.89}}

示例2说明：字幕识别误差（SWOT→sort）也计入相似度补偿。

# 待处理数据
[句子1]
{result_text}

[句子2]
{yuan_text}

请严格按照示例格式输出JSON，不使用换行符或特殊分隔符'''
